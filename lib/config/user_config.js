/* Deprecated
class UserConfig {
  constructor() {
    this.user_name=null; //JSON.parse(jsonStr)['user'];
    this.secret=null; //=JSON.parse(jsonStr)['password'];
  }
  secret(pw) {
    this.secret=pw;
    return this;
  }
  user(user_name) {
    this.user_name=user_name;
    return this;
  }
}

module.exports = UserConfig;
*/